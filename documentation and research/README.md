# documentation and research/

Session-scoped research + evidence folders, one per work session, named
`NN_short_topic` with a **sequential two-digit prefix**.  Any AI working on
this repo: take the next free number for your session's folder (02, 03, ...)
and leave everything you gathered (reference photos, renders, probe CSVs,
findings) inside it, plus a FINDINGS.md.  Older normative research docs live
in `docs/research/` — check both before re-researching anything.

- `01_missile_physics_and_models/` — 2026-07-06 overnight: missile flight
  physics audit (probe_missile_physics tool + the flown-weave fix), full
  render set of every missile model from side/top/BOTTOM/quarter
  (`renders/`), downloaded real-world reference photos (`references/`),
  and the model-correction pass notes.
